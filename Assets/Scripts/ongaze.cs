using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ongaze : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        printaj();
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public async void printaj()
    {
        Debug.Log("AAAAAA");
    }
}
